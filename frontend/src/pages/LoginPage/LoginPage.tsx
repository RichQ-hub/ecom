import TitleHeaderLayout from '../../layouts/TitleHeaderLayout/TitleHeaderLayout';
import LoginCard from '../../components/LoginCard/LoginCard';

const LoginPage = () => {
  return (
    <TitleHeaderLayout title="Login">
      <LoginCard />
    </TitleHeaderLayout>
  );
};

export default LoginPage;
